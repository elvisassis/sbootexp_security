spring:
  application:
    name: sbootexp_security
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sbootexp_security?useTimezone=true&serverTimezone=UTC
    username: root
    password: 123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
        hibernate:
    show-sql: false
    open-in-view: false
